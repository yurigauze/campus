import 'package:flutter/material.dart';

class TurmasProfessor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Turmas"),
      ),
      body: null,
    );
  }
}
