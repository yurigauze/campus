import 'package:flutter/material.dart';

class DispararTurnosProfessor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Disparar para Turno"),
      
      ),
      body: null,
    );
  }
}
