import 'package:flutter/material.dart';

class DispararAlunosProfessor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Disparar para Alunos"),
        
      ),
      body: null,
    );
  }
}
