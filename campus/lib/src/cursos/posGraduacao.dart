import 'package:flutter/material.dart';

class PosGraduacao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Pos Graduação")), body: null);
  }
}
